---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-with-proxy-http-version-unsupported
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/proxy-http-version: "1.0"

spec:
  ingressClassName: nginx
  rules:
    - host: proxy-http-version-unsupported.localhost
      http:
        paths:
          - path: /
            pathType: Exact
            backend:
              service:
                name: whoami
                port:
                  number: 80
