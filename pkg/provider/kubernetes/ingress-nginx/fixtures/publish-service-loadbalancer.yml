kind: Service
apiVersion: v1
metadata:
  name: traefik-publish
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: traefik
  ports:
    - name: https
      protocol: TCP
      port: 443
      targetPort: 8443
status:
  loadBalancer:
    ingress:
      - ip: 203.0.113.10
