---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: baqup-internal
  labels:
    name: baqup-internal
spec:
  controllerName: baqup.io/gateway-controller

---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: baqup-internal
  namespace: default
spec:
  gatewayClassName: baqup-internal
  listeners:
    - name: http
      protocol: HTTP
      port: 9080
      allowedRoutes:
        namespaces:
          from: Same

---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: baqup-external
  labels:
    name: baqup-external
spec:
  controllerName: baqup.io/gateway-controller

---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: baqup-external
  namespace: default
spec:
  gatewayClassName: baqup-external
  listeners:
    - name: http
      protocol: HTTP
      port: 9080
      allowedRoutes:
        namespaces:
          from: Same
