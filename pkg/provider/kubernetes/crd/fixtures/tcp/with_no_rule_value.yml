apiVersion: traefik.io/v1
kind: IngressRouteTCP
metadata:
  name: test.route
  namespace: default

spec:
  entryPoints:
    - foo

  routes:
  - match: ""
    services:
    - name: whoamitcp
      port: 8000
