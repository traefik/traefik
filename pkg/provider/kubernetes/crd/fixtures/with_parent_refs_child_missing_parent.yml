---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: child-missing
  namespace: default

spec:
  parentRefs:
    - name: nonexistent-parent
      namespace: default
  routes:
    - match: Path(`/missing`)
      kind: Rule
      services:
        - name: missing-service
          port: 9000