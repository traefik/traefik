kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: ingress-with-nil-endpointslice-port-value
  namespace: default

spec:
  rules:
  - host: whoami.localhost
    http:
      paths:
      - path: /
        backend:
          service:
            name: whoami-nil-port-value
            port:
              number: 80
        pathType: Prefix

---
kind: Service
apiVersion: v1
metadata:
  name: whoami-nil-port-value
  namespace: default

spec:
  ports:
    - name: web
      port: 80
  clusterIP: 10.0.0.1

---
kind: EndpointSlice
apiVersion: discovery.k8s.io/v1
metadata:
  name: whoami-nil-port-value-abc
  namespace: default
  labels:
    kubernetes.io/service-name: whoami-nil-port-value

addressType: IPv4
ports:
  - name: web
endpoints:
  - addresses:
      - 10.10.0.1
    conditions:
      ready: true
