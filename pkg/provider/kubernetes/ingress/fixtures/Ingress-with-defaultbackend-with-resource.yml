kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: defaultbackend
  namespace: testing

spec:
  defaultBackend:
    resource:
      apiGroup: example.com
      kind: SomeBackend
      name: foo
