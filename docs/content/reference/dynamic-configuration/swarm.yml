- "traefik.enable=true"
# `traefik.docker.network` is deprecated. Please use `traefik.swarm.network` instead.
- "traefik.docker.network=foobar" 
# `traefik.docker.lbswarm` is deprecated. Please use `traefik.swarm.lbswarm` instead.
- "traefik.docker.lbswarm=true" 
