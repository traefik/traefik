apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: baqup-keep
spec:
  controller: baqup.io/ingress-controller

---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: "whoami-keep-route"
spec:
  ingressClassName: "baqup-keep"
  rules:
  - host: "whoami.test.keep"
    http:
      paths:
      - path: "/keep"
        backend:
          service:
            name: whoami
            port:
              number: 80
        pathType: Prefix

---
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: baqup-drop
spec:
  controller: baqup.io/ingress-controller

---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: "whoami-drop-route"
spec:
  ingressClassName: "baqup-drop"
  rules:
  - host: "whoami.test.drop"
    http:
      paths:
      - path: "/drop"
        backend:
          service:
            name: whoami
            port:
              number: 80
        pathType: Prefix

---
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: baqup-not-ingress-controller
spec:
  controller: not.tr43phic.io/ingress-controller

---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: "whoami-drop-ingress"
spec:
  ingressClassName: "baqup-not-ingress-controller"
  rules:
  - host: "whoami.test.not.ingress"
    http:
      paths:
      - path: "/ingress"
        backend:
          service:
            name: whoami
            port:
              number: 80
        pathType: Prefix
